{
  "type": "module",
  "scripts": {
    "test": "mocha",
    "tdd": "npm test -- -R min -w -p",
    "cover": "c8 -r lcov -r html -r text npm test"
  },
  "devDependencies": {
    "c8": "^7.12.0",
    "chai": "^4.3.6",
    "mocha": "^10.0.0"
  }
}
